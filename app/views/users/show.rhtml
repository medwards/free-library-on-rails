<h1><%=h @user.login %></h1>
<% if @user.about %>
	<p class="description"><%= h @user.about %></p>
<% end %>

<table>
  <tr><th>type</th><th>title</th></tr>
  <% @user.owned.each do |item|
       type = item.class.to_s.downcase %>
    <tr>
      <td><%=h type %></td>
      <td><%= link_to highlight(h(item.title), @query), polymorphic_path(item) %></td>
    </tr>
  <% end %>
</table>
